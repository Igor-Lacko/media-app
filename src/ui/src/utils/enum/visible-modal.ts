/**
 * Enum of possibly visible models used in a detail page.
 */
export enum VisibleModal {
    NONE,
    DELETE,
    RATE,
    DESCRIPTION,
    WATCH_STATUS,
    PLAY_NOFILE,
    PLAY_WRONG_FILE,
}

export default VisibleModal;